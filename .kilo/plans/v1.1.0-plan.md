# v1.1.0 — REFACTOR (Plan Final)

## Ringkasan
9 track besar. Judul changelog: REFACTOR. Version lompat dari v1.0.20 ke v1.1.0.

---

## Track 1: Inventory Item Bug (Fix)
Investigasi kemungkinan residual bug sebelum v1.0.19 fix `g_MysqlRaceCheck`. Audit enum E_PLAYERS untuk overlap offset antara item slots (pSnack, pSprunk, dll) dengan pill slots (pCoughPills, pMigrainPills, pFiverPills). Tambah guard MysqlRaceCheck.

## Track 2: Maskot (Masuk Kota) — Vehicle Auto-Spawn Fix
**File:** vehicle/Function.inc:420
Cek jarak kendaraan dari spawn player saat login. Jika > 30.0m, set cDespawn=1, kirim pesan informasikan /spawn.

## Track 3: KEY_CTRL_BACK (Freeze Animasi)
**File:** main.pwn:982-989 — UNCOMMENT kode yang sudah ada. Tekan CTRL = berhenti animasi + reset special action.

## Track 4: Radio/vradio → boombox/vsong Refactor
/radio → /boombox (place only, no listen). /vradio → /vsong (vehicle only, auto-stop on exit).

## Track 5: Modshop Object Bug
Object lama hilang saat beli baru. Fix: destroy old object before create new. Cek MAX_VEHICLE_OBJECT limit.

## Track 6: Camera Bug Saat Menyetir
Camera zoom pas berhenti. Cari pTask atau callback yang reset camera saat velocity=0.

## Track 7: Vehicle Spawn Bug
Error "Cannot spawn vehicles that have not spawned". Fix: set cDespawn=1 untuk vehicle yang tidak di-create (terkait Track 2).

## Track 8: HBE Color by Percentage
61-100% = Hijau (~g~), 31-60% = Kuning (~y~), 0-30% = Merah (~r~). File: pTask.pwn:846-872.

## Track 9: House Sealing & Auction
Field baru: segel, segel_time, segel_admin, segel_reason, auction_time. Commands: /segelrumah, /tebusrumah, /lelangrumah, /infosegel. Admin 6+. Inactive 15+ hari. Tebus 7 hari bayar persentase. Lelang reset penuh.

## Track 10: Economy Refactor
Tabel `economy_config` (key, value, desc). Command /economy dialog multi-level (Admin 6+). Semua value dollar cents. FormatMoney display. Hardcoded payout diganti GetEconomyValue().

---

## Priority
P1: Track 1 (Inventory), Track 2 (Maskot), Track 3 (KEY_CTRL_BACK)
P2: Track 4 (Radio/vsong), Track 7 (Spawn), Track 8 (HBE Color)
P3: Track 5 (Modshop), Track 6 (Camera)
P4: Track 9 (House Seal), Track 10 (Economy)
