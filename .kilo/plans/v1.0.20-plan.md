# Plan v1.0.20 — Final Items

## Summary
Lima permintaan player untuk v1.0.20, satu (`/showlic`) sudah selesai.

---

## Task 1: Radius boombox 30.0 → 15.0
**File:** `gamemodes/main.pwn:2172`
Ubah argumen ke-5 `PlayAudioStreamForPlayer` dari `30.0` → `15.0`.

---

## Task 2: Phone Book product di Electronic Store (restock owner)
**File:** `gamemodes/Modules/Property/bisnis/Dialog.inc`
Tambah case 4 di blok `bType == 5`.

Produk beli pPhoneBook=1 -> ShowContacts(). Guard pPhoneBook>0.

**SQL migration (copas ke phpMyAdmin):**
```sql
UPDATE `bisnis`
SET `bpname4` = 'Phone Book', `bprice4` = 500,
    `bussinesstock` = CONCAT(SUBSTRING_INDEX(`bussinesstock`, '|', 4), '|50', '|', SUBSTRING_INDEX(`bussinesstock`, '|', -6))
WHERE `type` = 5 AND (`bpname4` IS NULL OR `bpname4` = '' OR `bpname4` = '0' OR `bpname4` = '-');
```

---

## Task 3: Auto-RP `rpperiksa` SAMD (exact lowercase)
**File:** `gamemodes/main.pwn` — `TryAutoRoleplay`
`strcmp(text, "rpperiksa", false)`. SAMD -> cari nearest player 5m -> Health_Show. Non-SAMD return 0.

---

## Task 4: Dynamic Pickup (model 1239) + 3D Label di Milker
**File:** `gamemodes/Modules/Jobs/Milker/SAMPCallback.inc` — `hook OnGameModeInit()`
Tambah pickup+label untuk Locker, 6 cows, Drop-off. Stream distance 30.0.

---

## Task 5: `/showlic` — SUDAH ADA di PlayerCommand.inc:2976

---

## Validasi
Compile: `pawncc main.pwn -ipawno/include` -> 0 errors
